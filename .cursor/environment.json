{
  "name": "FishTheFlats",
  "install": "npm install && npx prisma migrate deploy && npm run seed",
  "start": "npm run dev -- --hostname 0.0.0.0 --port 3000",
  "terminals": [
    {
      "name": "dev-server",
      "command": "npm run dev -- --hostname 0.0.0.0 --port 3000"
    }
  ],
  "ports": [3000]
}
